model Posts {
    id        Int       @unique @default(autoincrement())
    data      Json?
    name      String
    createdAt DateTime  @default(now()) @db.Timestamp(0)
    deletedAt DateTime?
}

// model ExampleResults {
//     id          Int    @unique @default(autoincrement())
//     name        String
//     classCode   String
//     subjectCode String
//     score       Int?
//     obtainable  Int
// }
